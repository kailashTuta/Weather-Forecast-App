apiVersion: v1
kind: Service
metadata:
  name: weather-service
  labels:
    app: weather
spec:
  type: NodePort
  selector:
    app: weather
    version: blue
  ports:
    - port: 80
      targetPort: 80
